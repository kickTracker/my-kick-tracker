{
  "name": "kick-username-tracker",
  "version": "1.0.0",
  "main": "tracker.js",
  "type": "module",
  "scripts": {
    "start": "node tracker.js --monitor",
    "run-once": "node tracker.js",
    "check-streamers": "node tracker.js --check-streamers",
    "refresh-all": "node tracker.js --refresh-all",
    "prune": "node tracker.js --prune",
    "archive": "node tracker.js --archive",
    "monitor": "node tracker.js --monitor",
    "serve": "node server.js",
    "gen-icons": "node generate-icons.js",
    "postinstall": "node generate-icons.js || true"
  },
  "dependencies": {
    "puppeteer": "^22.0.0",
    "sqlite3": "^5.1.7"
  }
}
